name: walkinsalonapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # 🌐 Core dependencies
  intl: any
  cupertino_icons: any

  # 🔥 Firebase
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_storage: ^13.0.3
  firebase_messaging: ^16.0.3

  # 🪣 Supabase
  supabase_flutter: ^2.10.3

  # 📍 Location & Maps
  geocoding: ^4.0.0
  geolocator: ^14.0.2
  flutter_map: ^8.2.2
  latlong2: ^0.9.1

  # 🧰 Utilities
  http: ^1.5.0
  uuid: ^4.5.1
  path_provider: ^2.1.5
  image: ^4.5.4
  file_picker: any
  flutter_image_compress: ^2.4.0
  flutter_cache_manager: ^3.4.1

  # 💅 UI / Styling
  google_fonts: ^6.3.2
  responsive_framework: ^1.5.1
  flutter_animate: ^4.5.2
  palette_generator_master: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # ✅ Assets (include your whole images directory)
  assets:
    - lib/assets/images/

  # 🖋️ Fonts (optional since you're using GoogleFonts)
  # If you want to keep Roboto as a fallback:
  fonts:
    - family: Roboto
      fonts:
        - asset: lib/assets/fonts/Roboto-Regular.ttf
        - asset: lib/assets/fonts/Roboto-Bold.ttf
